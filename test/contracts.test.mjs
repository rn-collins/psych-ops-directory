import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const r=p=>readFile(new URL("../"+p,import.meta.url),"utf8");test("no write or admin routes",async()=>{for(const p of["api/admin.js","api/seed.js","api/submit.js","api/flag.js","api/track.js","api/analytics.js","api/subscribe.js"])await assert.rejects(r(p))});test("safe public API fields",async()=>{const s=await r("api/listings.js");for(const x of["price","svc","note","tier"])assert.doesNotMatch(s,new RegExp(x+":"))});test("strict CSP",async()=>assert.doesNotMatch(await r("vercel.json"),/unsafe-inline|unsafe-eval/));test("bounded language",async()=>{const s=await r("public/index.html");assert.match(s,/not vetting, endorsement/);assert.doesNotMatch(s,/vetted vendors/i)})